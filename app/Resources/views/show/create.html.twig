{% extends 'base.html.twig' %}
{% set action = app.request.attributes.get('_route') | split('_') | last | capitalize%}

{% block pageHeading %}
        {{ action }} a show
{% endblock %}
{% block content %}

    {#{{ form(showForm) }}#}

    {{ form_start(showForm, {"attr":{"novalidate":"novalidate"}}) }}
        {{ form_widget(showForm) }}
        <input type="submit" value="{{ action }}">
    {{ form_end(showForm) }}

{% endblock %}



