# To get started with security, check out the documentation:
# https://symfony.com/doc/current/security.html
security:

    # https://symfony.com/doc/current/security.html#b-configuring-how-users-are-loaded
    providers:
        chain_provider:
            chain:
                providers: [in_memory, user_db]
        in_memory:
            memory:
                users:
                    random.user@memory.com:
                        password: '$2y$13$WQJGpnlJ2T6z0C5FI6t34OQv7sVaO9Ey/IwJ4wUIj8J087bDFWgli' #user
                        roles:    'ROLE_USER'
        user_db:
            entity:
                class: AppBundle\Entity\User
                property: email

    encoders:
        Symfony\Component\Security\Core\User\User: bcrypt
        AppBundle\Entity\User: bcrypt   #or argon2i for php7.2+

    firewalls:
        # disables authentication for assets and the profiler, adapt it according to your needs
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false

        anonymous_area:
            pattern: ^/login$
            anonymous: ~

        secured_area:
            pattern: ^/
            provider: chain_provider
            form_login:
                login_path: /login
                check_path: /login_check
                username_parameter: email         #overrides _username key
                password_parameter: password      #overrides _password key
                always_use_default_target_path: true
            logout: ~

        main:
            anonymous: ~
            # activate different ways to authenticate

            # https://symfony.com/doc/current/security.html#a-configuring-how-your-users-will-authenticate
            #http_basic: ~

            # https://symfony.com/doc/current/security/form_login_setup.html
            #form_login: ~
